import React from "react";

export const SubHeading = ({ label }) => {
  return (
    <h4 className="mx-auto my-2 px-5 text-xl font-semibold text-gray-600 text-center">
      {label}
    </h4>
  );
};
